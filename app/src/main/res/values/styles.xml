<resources>
    <!-- Estilo de esquinas redondeadas para ShapeableImageView -->
    <style name="RoundedCornersImage" parent="ShapeAppearance.Material3.Corner.Medium">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Tema por defecto mínimo (evitar conflictos) -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Personalizaciones mínimas -->
    </style>
</resources>
